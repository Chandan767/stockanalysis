services:
  # Single Unified Full-Stack Web Service (FastAPI Backend + React Frontend SPA)
  - type: web
    name: stockanalysis
    env: python
    region: oregon
    plan: free
    buildCommand: cd frontend && npm install && npm run build && cd ../backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./stock_db.db
      - key: PYTHON_VERSION
        value: 3.11.8
